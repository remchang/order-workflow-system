# 库存服务依赖 —— 按本机 pip freeze 的实际结果写死版本
# 生成方式： .\.venv\Scripts\python -m pip freeze

fastapi==0.141.1
uvicorn==0.53.0
starlette==1.6.0
pydantic==2.13.5

# 仅测试用（FastAPI 的 TestClient 需要 httpx）
httpx==0.28.1
pytest==9.1.1

# 传递依赖
annotated-types==0.8.0
anyio==4.15.1
certifi==2026.7.22
click==8.5.0
colorama==0.4.6
h11==0.16.0
httpcore==1.0.9
idna==3.19
iniconfig==2.3.0
packaging==26.3
pluggy==1.6.0
pydantic_core==2.46.5
Pygments==2.21.0
typing_extensions==4.16.0
typing-inspection==0.4.4

# 说明：文档里建议的 fastapi 0.115.12 / uvicorn 0.34.3 在 Python 3.13
# 环境里没有可直接安装的组合，实际装到的是上面这些版本。
# 代码没有依赖任何被移除的 API，测试全过。差异记在 docs/ceshi-jilu.md。
